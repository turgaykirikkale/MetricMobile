import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  mainContainer: {
    flexDirection: 'row',
    // marginHorizontal: 10,
    marginTop: 10,
    marginHorizontal: 10,
  },
});
